#built-in modules are the ones that come pre-installed with Python.
#we don’t need to install them separately — just import and use.

import math
print(math.sqrt(16))       # 4.0
print(math.factorial(5))   # 120
print(math.pi) 